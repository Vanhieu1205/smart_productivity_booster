<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================
         PERMISSIONS – Quyền cần thiết cho Background Service & Notifications
         ============================================================ -->

    <!-- Foreground Service: bắt buộc để chạy background timer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Loại foreground service (Android 14+): khai báo cụ thể loại dùng -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Đánh thức thiết bị khi cần gửi notification (màn hình đang tắt) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Gửi notification (Android 13+ cần xin explicit permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Tự khởi động lại service sau khi thiết bị reboot (tùy chọn) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Giữ kết nối network trong background (nếu cần sync dữ liệu) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="smart_productivity_booster"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ============================================================
             BACKGROUND SERVICE – flutter_background_service
             Khai báo service để Android biết đây là Foreground Service hợp lệ.
             android:exported="false": chỉ app này mới start được service này.
             ============================================================ -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

